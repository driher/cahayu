import "./globals.css";

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="id">
      <body>{children}</body>
    </html>
  );
}

export const metadata = {
  title: "Radio Sehati",
  description: "Radio Streaming Sehati",
  manifest: "/manifest.json",
};